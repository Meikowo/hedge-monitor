# worklog_2026-07-17-3 - 首批 LLM 抽取验收

## 1. 本次阶段

验证 `Extract Batch (LLM) #32` 是否完成首批结构化抽取，并检查抽取结果、证据字段和事件层派生结果。

## 2. 验收结果

- workflow 状态为 `Success`，运行时长约 1 小时 56 分钟。
- `extractions` 新增并累计为 360 条。
- 360 条抽取均为 `is_hedge_related=true`。
- 360 条均有 `text_chars` 和 `pdf_pages`，说明正文/PDF 证据链已写入。
- `announcements` 状态：`extracted` 360 条，`pending` 3,166 条，暂无 `failed`。
- `hedge_events`：178 条；`event_members`：360 条，事件层已自动重建。
- 抽取角色分布：计划-董事会 166、可行性分析 104、管理制度 75、进展 8、其他 6、计划-股东大会 1。

## 3. 决策

1. 首批结果通过基础验收，继续按 `limit=300` 重复运行 `Extract Batch (LLM)`。
2. 每轮运行后核对 `extracted/pending/failed`、证据字段和事件数量；不要开启 `retry_failed`，直到出现真实失败项再处理。
3. pending 清零后执行完整 `verify.sql`，再抽样检查额度口径和引用证据，最后收口 R1。

## 4. 阶段结论

MiniMax 在 GitHub Actions 上已完成真实批次抽取，抽取与事件派生链路均正常。当前瓶颈是剩余 3,166 条公告的批量处理时间，而不是配置或数据库连接问题。
