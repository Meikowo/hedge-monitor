# Worklog 2026-07-18 (M3 encoding and density fix)

## Goal

Fix Chinese mojibake in the GitHub Pages preview and move the interface toward a white, dense research-terminal layout.

## Completed

1. Identified the root cause: the first connector upload transcoded Chinese text before writing it to GitHub; this was not a browser font or Supabase encoding issue.
2. Re-synced `web/index.html`, `web/app.js`, and `web/styles.css` as raw UTF-8 and verified the Chinese content on GitHub.
3. Tightened the layout: smaller masthead and metric cards, reduced sidebar/content spacing, shorter event rows, finer borders, and stronger table hierarchy while preserving mobile stacking.
4. Kept the browser bundle limited to the Supabase publishable/anon key; no service-role credential was added.
5. Merged the fix through PR #9.

## Validation

- GitHub file content now displays Chinese correctly.
- `v_ann_flow` and `v_events` remain available through the public read-only endpoints.
- The Pages workflow is present; the repository still needs Settings -> Pages -> Source = GitHub Actions for the first public deployment.

## Next

After Pages is enabled, run `Deploy Hedge Monitor Preview` and review the desktop/mobile rendering. Then refine table columns, filters, and the event detail layer.
