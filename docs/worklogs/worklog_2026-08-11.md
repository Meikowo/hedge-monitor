# Worklog 2026-08-11

## 本阶段目标

- 将已确认的风险案例监控设计接入正式网站，不再用 Demo 数据。
- 收紧 Tavily 媒体线索公开闸门，撤回已有误报。
- 将风险表的浏览器权限统一收紧为最小只读权限。

## 媒体发布质量修复

- 新增公司身份一致性校验：正文明确出现其他股票代码，或全文没有当前公司代码/名称时，
  不得沿用搜索阶段预分配的上市公司身份。
- 新增已发生事实与重要性校验：制度说明、风险因素、假设性损失、一般汇率分析和
  “套保不等于没有汇兑损失”等评论不构成风险案例；损失类记录须有实际金额、比例、重大
  表述或爆仓/强平等明确事实。
- 新增历史公开记录 reconciliation：若一个已发布媒体事件关联的所有私有来源都不再通过
  新闸门，将其状态改为 `dismissed`；重复运行不会重复新增或误撤仍有合格来源的事件。
- 生产预演共读取 39 条候选：2 条通过、37 条排除、2 条既有误报待撤回。写入后保留
  江特电机、仙乐健康，撤回包钢/惠科错配和玲珑轮胎泛分析；原始私有审计记录未删除。

## 数据库权限

- 新增 `db/009_risk_public_readonly.sql` 并应用到生产 Supabase。
- `risk_source_documents`、`derivative_risk_cases`、`risk_case_documents`、
  `risk_case_evidence`、`risk_media_reports`、`risk_media_report_sources` 对浏览器角色仅有
  `SELECT`。
- `risk_media_leads`、`risk_media_backfill_windows` 对浏览器角色无任何表权限；八张风险表
  全部启用 RLS。Supabase Security Advisor 检查为 0 条告警。

## 正式前端

- 左侧工作区新增“风险案例监控”，资源版本升级为 `v=20260811-1`。
- 新增 `web/risk.js`，读取官方四表与公开媒体两表，绝不读取私有媒体线索/回填窗口。
- 官方案例与媒体记录统一归一化，官方记录优先并去除已关联或同公司同风险近日期媒体重复。
- 页面提供 4 项指标、6 组筛选、高密度表格、详情抽屉、官方逐字段证据、媒体来源链接、
  CSV 导出、加载/重试/错误/空状态和移动端布局。
- 本地浏览器验收：0 条官方案例、2 条媒体记录；2025 年筛选为 1 条；详情可见新浪财经
  来源和“媒体记录尚无官方逐字段证据”；390px 视口无页面横向溢出。

## 验证结果

- Python：188 项测试通过。
- Node 离线：定期报告 Demo/正式逻辑、风险 Demo/正式逻辑、正式站结构共 7 组通过。
- 真实 API：主站读取 9,221 个事件和 20,546 条结构化公告；风险页读取 0 条官方案例、
  2 条公开媒体记录和 2 条来源，约 2.1 秒完成。
- 匿名访问两张私有媒体表返回权限错误；公开接口、RLS 和安全顾问检查通过。

## 下一步

- 发布当前正式前端并在线核验自定义域名资源版本、风险入口、筛选、详情和导出。
- M6a 继续以媒体线索反查和官方渠道扫描形成 1—3 个真实官方案例纵向切片，再逐步扩展
  到 50 份官方候选、10 个正式案例和不低于 90% 候选精确率。
