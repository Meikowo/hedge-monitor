# Worklog 2026-07-18（待推送更新合并与恢复顺序）

## 已完成

1. 将昨日未推送的前端密排改版、事件主键修复、独立 `Build Events` workflow 和工作记录同步到 GitHub。
2. PR #11 已合并到 `main`。
3. 远程文件 UTF-8、前端文案、事件 key 修复和工作流入口均已复核。

## 当前数据库快照

- announcements: `extracted=1067`、`pending=2378`、`failed=85`、`irrelevant=1`、`skipped=1`
- extractions: `1068` 行，其中 `is_hedge_related=true` 为 `1067` 行
- quota_items: `1516` 行
- hedge_events: `0` 行
- event_members: `0` 行

事件层为 0 是上一次全量重建先清空派生表、随后因重复 event_key 失败造成的；公告层、抽取层和额度明细仍完整。

## 下一步

1. GitHub Actions 手动运行 `Build Events`（main），不调用 LLM。
2. 成功后复核 `hedge_events` 与 `event_members` 数量，并检查 Pages 事件列表恢复。
3. 继续 `Extract Batch (LLM)`：`limit=300`、日期留空、`retry_failed=false`，直到 pending 清零。
4. pending 清零后，用 `retry_failed=true`、`limit=30` 小批验证旧失败项，再逐批清零 failed。
5. 运行 `db/verify.sql` 收口 R1，再进入 2025→2021 历史回填和 M3 前端正式功能。
