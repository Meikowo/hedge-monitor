# Worklog 2026-08-01

## M6a Tavily 新闻风险线索 POC

- 用户已创建 Tavily 免费账号，并把 `TAVILY_API_KEY` 保存为 GitHub repository secret。
- 新增私有 `risk_media_leads` 数据域。媒体报道只作为未核实线索，不进入官方来源文档或
  正式风险案例四表，不向 anon/authenticated 或当前前端开放。
- 搜索固定为 Tavily news/basic，不请求答案、全文或图片；每天两次、每次最多 12 个
  查询，按 31 天计算月度理论上限 744 credits，低于免费计划 1,000 credits。
- 结果须在标题或同一句局部语境内同时命中衍生品词和风险词，正文还须有已发生语义；
  “若、可能、一旦”等假设性风险提示排除。URL 去除跟踪参数并按哈希幂等去重，多查询
  命中合并；写库时按代码或最长公司全称匹配公司主表。
- 手动 workflow 默认只运行 1 个查询、返回最多 3 条、`write=false`；定时任务才使用
  完整额度并幂等写入。缺少 Key 时立即失败且不回显密钥，不调用 MiniMax。
- 由于 GitHub 插件没有首次 dispatch 接口且本地/浏览器无登录态，采集器、查询配置或
  workflow 本身变更时自动执行 1-query、3-result、`write=false` 冒烟测试，避免人工卡住验收。

## 验收记录

- TDD 首轮 8 项测试按预期因迁移、采集器和 workflow 缺失而失败；实现后 8/8 通过。
- 初版完整回归 129/129 通过，Python 语法及 workflow/query YAML 校验通过；仓库文件未发现 Tavily 实际密钥。
- Supabase 已应用 `m6a_risk_media_leads` 与 `m6a_risk_media_leads_service_policy` 两项迁移：新表初始 0 行，RLS 开启，anon/authenticated 无读取权限，service_role 可访问；安全顾问复检 0 项。
- GitHub 首次原子提交 `c1178347` 与自动冒烟提交 `9d0370d6` 均已非强制快进到 `main`。
- 真实冒烟 run `30685805975` 成功：Secret 有效，1 credit，初筛 2 条，`write=false`。
  两条均为天原股份发行材料中的假设性套保风险提示，不是已发生案例；据此增加同句局部
  语境、已发生语义和假设语气排除规则及回归样本。修正后完整回归 130/130 通过。
- 质量修正提交 `e56bb49a` 已非强制快进到 `main`；修复后冒烟 run `30686071872`
  成功，1 credit、保留 0 条、`write=false`。Supabase 复查 `risk_media_leads` 仍为 0 行。
