# worklog_2026-07-17 —— R0 部署验收与公司维表导入

## 1. 事件与根因

本阶段将 2026-07-13 交付的 R0 底座部署到新 Supabase，并把本地 R0 版本替换到 GitHub 仓库 `main`。旧版 GitHub 项目先创建 `legacy-demo` 分支保留，旧 Supabase 项目不触碰。

## 2. 决策

1. 新建 Supabase 项目 `hedge-monitor-r0`（区域 `ap-southeast-1`），不复用旧项目。
2. 采用仓库 `db/001_init.sql` 作为唯一数据库事实源，直接应用到新项目。
3. GitHub 先建立 `legacy-demo`，再通过 `r0-publish` 草稿 PR 将 R0 文件树合并到 `main`。
4. 抽取和采集优先使用 GitHub Actions；本机 Python 环境未进入当前 Codex 终端 PATH，不作为本阶段阻塞项。

## 3. 交付

- 新 Supabase 项目已创建，R0 六张表已建立，全部启用 RLS。
- GitHub PR #1 已合并到 `main`：R0 脚本、数据库迁移、配置、工作流、文档和 iFind 公司表已入库。
- `legacy-demo` 分支保留旧版项目。
- GitHub Secrets 已配置，MiniMax Reachability workflow 成功。
- `Import Companies (iFind)` 的 `dry_run=true` 与 `dry_run=false` 均成功。

## 4. 验收

新 Supabase `companies` 表验收结果：

- 总数：5524
- 民企：3493
- 地方国企：1049
- 央企：479
- 外资：342
- 其他：131
- 集体：30
- 市场分布：BJ 326、SH 2306、SZ 2892

`dry_run=false` 已完成真实写入。GitHub Actions 中的 Node.js 20 弃用提示仅为 warning，不影响本次导入。

## 5. 当前进度

- R0.1 数据库底座：完成并验收。
- R0.2 自动化管线与工作流：已合并 `main`。
- R0.3 用户侧部署：核心步骤完成。
- R0.4 MiniMax@Actions 探活：完成。
- R1 历史数据初始化：进行中，下一步回填 2026 公告。

## 6. 下一步

1. 运行 `Backfill Announcements`，年份设为 2026。
2. 检查公告数量和日期覆盖。
3. 运行 `Extract Batch (LLM)`，逐批清理 pending。
4. 运行事件层重建并执行 `verify.sql` V4–V9。
5. R1 验收完成后，再开始前端 M3。

## 7. 回滚说明

GitHub 旧版本仍在 `legacy-demo`；旧 Supabase 项目未修改。新项目数据可由 iFind 公司表、公告回填和抽取管线重建。
