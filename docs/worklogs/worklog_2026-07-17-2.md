# worklog_2026-07-17-2 - 2026 公告回填验收

## 1. 本次阶段

推进 R1 数据初始化，验证 GitHub Actions `Backfill Announcements` 是否已把 2026 年公告写入新 Supabase 的 `announcements` 表。

## 2. 验收结果

- workflow `Backfill Announcements #2` 状态为 `Success`，指定年份 2026 的回填已完成。
- `announcements` 总数：3,526 条。
- 日期范围：2026-01-01 至 2026-07-15。
- 当前 `status` 全部为 `pending`，符合等待抽取的预期。
- `extractions` 当前为 0 条，说明还未开始抽取，不是回填失败。
- 来源分布：`title:套期保值` 2,375 条；`title:外汇衍生品` 524 条；`title:衍生品交易` 316 条；其余关键词来源合计 311 条。

## 3. 决策

1. 保持 `announcements` 的 `pending` 状态，用 `Extract Batch (LLM)` 按批次抽取。
2. 首批先用 `limit=300` 做小规模验收；确认结果后重复运行，直到 pending 清空。
3. 抽取完成后再运行事件层重建与 `verify.sql`，最后进入前端 M3，避免在数据未验收前固化页面口径。

## 4. 阶段结论

2026 公告回填已成功完成；当前工作重点已从“采集/回填”转移到“LLM 结构化抽取”。GitHub Actions 页面出现的 Node.js 20 deprecation 仍只是 warning，不影响本次成功结果。
