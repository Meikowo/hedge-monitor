# Worklog 2026-07-30

## M6a 衍生品风险案例基础切片

- 按已确认的独立风险案例库方案启动 M6a，不修改公告事件、年报事实表和生产前端。
- 新增 `db/005_derivative_risk_cases.sql`：
  - 官方来源文档 `risk_source_documents`；
  - 正式风险案例 `derivative_risk_cases`；
  - 案例—文档关系 `risk_case_documents`；
  - 字段级官方证据 `risk_case_evidence`。
- 四表全部启用 RLS；anon/authenticated 只读，service_role 写入。`000_reset.sql` 与
  `verify.sql` 已同步更新。
- Supabase 已应用迁移 `m6a_derivative_risk_cases`（版本 `20260730054652`）。验收结果：
  四表 RLS=true、四条匿名只读策略、service_role 16 项 CRUD 权限、四表均为 0 行；
  Security Advisor 0 告警。Performance Advisor 仅提示空表新索引尚未使用，属于预期
  INFO，不在尚无查询负载时删除。

## 上交所官方来源 POC

- 新增 `scripts/risk_sources/sse.py`，使用上交所监管信息公开接口，支持监管问询和监管
  措施，处理 JSON/JSONP、两种结果结构、UTF-8、分页、重试、官方 URL 规范化。
- 新增 `scripts/risk_relevance.py`，以确定性规则做首层相关性筛选；候选不自动成为正式
  案例。
- 新增 `scripts/fetch_risk_documents.py`：
  - 默认 dry-run；
  - 可下载 PDF/HTML 正文；
  - 生成带命中词和上下文片段的 CSV 快照；
  - 显式 `--write` 时按 `source_doc_id` 幂等 upsert；
  - 写入前校验公司主表代码，未知代码置空而不丢文档。
- 新增手动 `Risk Cases POC (SSE)` workflow。默认 `write=false`；写库必须再填写
  `I_UNDERSTAND`。POC 阶段不设 schedule、不调用 MiniMax。

## 真实来源验证与规则修正

- 真实读取并解析 75 份上交所监管措施与 75 份问询函正文；下载失败 1 份，未写数据库。
- 压力测试发现并修正以下非衍生品语境误报：
  - 监管决定固定尾注“证券期货市场诚信档案”；
  - 航运“舱位互换”；
  - 应收款远期结算与远期退换货；
  - 股票期权激励；
  - “过渡期权益”跨词粘连。
- 宽泛的“远期/期权/互换”改为金融语境短语，例如外汇远期、商品期权、利率互换。
  修正后上述 150 份普通监管材料不再误入候选。

## 验证

- M6 契约与规则测试 18 项通过，新增顶层结果分页、“候选但未在 320 字符窗口内确认”
  以及幂等写入参数的边界测试。
- 完整 Python 回归 121 项通过，M4 年报流程未受影响。
- 三个新增 Python 脚本通过 `py_compile`；GitHub Actions YAML 可解析。
- Supabase 迁移、安全边界和空库初态均已在线验收。
- POC workflow 的结束日期改为留空时自动使用运行当天，避免静态日期过期。

## 下一步

1. 接入 SZSE 与 CSRC/派出机构官方来源适配器。
2. 扩大 2024 年至今的来源扫描，形成至少 50 份真正的衍生品风险候选并分层抽检。
3. 接入 MiniMax 风险分类、八类案例结构化、官方引文与数值回验。
4. M6a 质量闸门通过后再写正式案例；M6b 才增加近五年回填、定时增量和前端入口。
