# 2026-09-11 — M5 真实样本核对与 2026H1 小样本验证

## 目标与边界

沿用 PRD 5.6 的三个交付步骤，本次先交付第一步：可重复运行的核对规则、
真实样本结果及复核材料。2026H1 复用已验证的六家公司试跑，不启动全市场中报
自动批量，不修改公告事实、不覆盖 2025 年报、不把预核对结果写成正式结论。

## 执行清单

- [x] `tests/test_plan_actual_rules.py` 先验证单位、零/空值、报告级与业务级、
  币种、授权期间、多额度及证据缺失的拒绝比较行为。
- [x] `scripts/plan_actual_rules.py` 实现无网络、无写库的确定性预核对规则。
- [x] `scripts/audit_plan_actual.py` 只读加载公告与报告事实，保存带来源 ID、
  原始引文、规则版本和原因的 JSON / HTML 复核材料；不覆盖任何源表。
- [x] `config/m5_validation_2025.csv` 选取混合类别、保证金、名义本金、
  年内调额等实际样本，执行真实数据核对。
- [x] 2026H1：复用 `config/annual_validation_2025.csv` 六家公司；metadata/locate
  指定 `--fiscal-year 2026 --report-type semiannual`；extract 指定相同参数和
  `--confirm-llm --dry-run`。核查期间、单位、期初期末及会计披露。
- [x] 完成测试、更新 PROJECT；发布采用 GitHub 插件非强制快进及逐文件哈希回验。

## 已确认基线

- GitHub main：`0f60aadb1dece352d22240d9790c275c37abb082`。
- 2025 年报全库：已提取 1769 份、失败 8、待 OCR 2、跳过 4。
  这是报告记录数，不等于正式池公司数。
- 开始前没有 2026H1 报告，也没有 M5 独立派生表。
- 当前前端 `matchStatus` 仍为“待口径核对”，尚未实施自动三维核对。
- 13:29：六份中报元数据写入、六份定位成功；随后试提取采用 dry-run，
  不自动出现在正式网站、不自动计入人工验收。

## 验收与交付

### M5

十二家公司、十九业务行；候选计划额度 68 条，其中 8 条的绝对日期授权期通过
PDF 原页全文匹配（不是仅核验引文前 40 字）。仅原文明示的两个日历日期可自动
解析，不能用 `anchor_year`、公告日或“年度股东大会”文字推断生效期。

十个不同数值候选：业务归属不明 4、期间缺证 3、单位未识别 1、误提授权额 2；
某些报告合计出现在多个业务行，仅供定位证据，不按多笔事实汇总。
本轮没有得到可直接发布的数值匹配结论，场所仍为证据不足。

- 大北农：期末保证金 116,653,692.30 元；候选上限 3 亿元，但期限为两次年度
  股东大会之间，缺少核实后的生效日期，不直接标“全年匹配”。
- 株冶：2025 年报中的 5.45 亿元与 32.70 亿元是上限，不应被当成实际峰值；
  两条既有事实保留不改，M5 明确拦截，后续按原页复核后单独处理历史记录。
- 海大：原文美元名义本金被记为 unit=其他，拒绝猜倍率。
- 锐新/焦作：铜材、铝材、铝锭等名称仍保留原文；在没有定稿业务别名映射前，
  差异只作复核提示，不作超范围定性。

产物（output 不推送）：

- `output/m5_sources_2025_20260911_134042.json`：只读原始输入。
- `output/m5_audit_2025_20260911_134418.json` / `.html`：初版复核输出（后续
  同一输入重算可能生成更新的时间戳文件，以最新文件为准）。

可复算命令：

```powershell
.\.venv\Scripts\python.exe scripts/audit_plan_actual.py --verify-periods
.\.venv\Scripts\python.exe scripts/audit_plan_actual.py --input output/m5_sources_2025_20260911_134042.json
```

### 2026H1 两轮试跑与修复

第一轮 13:30:15—13:32:04，六份、54 条候选；第二轮 13:41:01—13:42:59，
六份、44 条候选；没有触发模型失败熔断。全部 dry-run，没有写入正式事实表。

原页截图由代理直接生成并核验，位于 `output/h1-review/`，不再要求用户手动截图。

1. 大北农 PDF 第30页（印刷页29）的“募集资金”表被误继承前页衍生品表头。
   根因：旧续表仅检查相邻页和表格上沿，没有验证新表行内容及列数。
   修复：续接需要有衍生业务行且列数一致；新募集资金表不再生成六条伪衍生事实。
2. 株冶 PDF 第21页 5.45 亿元为计划上限，不是已使用峰值。
   修复：归一化与 M5 入口共用金额所在分句的上限判断；提示词版本递增。
   实际峰值后附“未超过授权额度”的评论不会被误删，真正的授权金额仍被拒绝。
3. 株冶外币报表折算差额不属于衍生品 OCI，增加明确排除。

回归保持原有单位换算测试的意图，但把原测试中误用的“授权上限”样例改为
明确“实际峰值”的引文，另加反例证明授权上限必须被拒绝。

### 尚未通过的核验项（不要据此开全市场中报批量）

- 中集：第二轮漏掉第一轮已找到的综合损益 -9,767 千元和外汇部分 -9,766 千元。
  不把调用成功等同于抽取完整。会计政策中的现金流量套期不自动视为本期已应用。
- 晶科：第二轮仅四条附注事实。18:47 已查明并修复定位漏页：PDF 第27页有
  商品/外汇明细，第28页有综合损益；明细表头和实际损益说明获得优先选页，
  仍保持最多15页。真实 PDF 本地重放已恢复10条表格事实（商品/外汇各5条），
  外汇公允价值变动损益3,968.54万元、商品-44,281.69万元与截图一致。
  此项为确定性表格重放，不是第三轮完整 LLM 抽取，综合损益完整性仍需验收。
- 株冶：第二轮漏提第20页综合损益 -26,058,614.68 元；第186页公允价值变动
  收益的资产端、负债端是两个真实分项，仍需补层级标签，不能无条件选第一项。
- 大北农：新旧两轮条数差异除删除错误表外也有综合损益召回波动。

这些是代理能继续查原页的技术问题，目前不要求用户逐页手工核验；若涉及业务
口径判断再附原文单独提问。新中报自动调度尚未启用。

### 测试与发布

`python -m unittest discover -s tests -p 'test_*.py'`：238 项通过，包含测试用故障
隔离/熔断产生的预期 WARN。新增 M5 核对、适配器、H1 防误提测试均先观察失败再修复。
远端 `tests/test_periodic.py` 为 30,012 字节的截断损坏版本，本地完整测试可运行，
本次一并以完整 UTF-8 文件恢复；不改其他无关文件。

只读代码复核发现的两个边界已先补失败测试再修复：授权说明不能抹掉实际峰值；
“外汇合约”等有效续表行应被接受。新增 `scripts/metric_evidence.py` 共用金额证据
防误判规则；募集资金表误接的拒绝测试保持通过。上述晶科漏页也有独立回归测试。
二次复核补充了换算金额和“货币资金”反例：金额判断移至原文单位恢复之后，
外汇/汇率/利率/货币续表标签要求具有合约语义，避免把货币资金表当衍生品。

无数据库 DDL，无现有事实回写，无前端/工作流配置变更；2026H1 库内六份均为
located 且 profiles=0。回滚代码通过新提交恢复上述基线相关文件，不强推；
不因回滚删除 2025 数据。中报试跑快照保存在本地 output/，重复试跑可重现但
LLM 输出不保证完全相同。
