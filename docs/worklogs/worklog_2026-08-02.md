# Worklog 2026-08-02

## M6 正式风险案例召回口径确认

- 用户确认风险案例监控本地 Demo 布局可用，正式实施继续沿用高密度主表与右侧证据抽屉，
  不在质量闸门前接入生产导航。
- 首批真实案例不限定最近五年；不再使用“官方证据最完整”这一主观筛选条件。凡官方原文
  直接涉及上市公司已发生的衍生品风险事实，均可先进入官方候选层。
- 媒体、搜索摘要和转载仍不能成为正式案例依据；它们只用于反查交易所、证监会/派出机构、
  公司法定披露公告和定期报告等官方原文。
- 首次 Tavily 历史回填已成功处理 2025 年 12 个年度窗口：70 条原始结果经保守门槛保留
  2 条私有线索；6 个饱和年度窗口拆为 24 个季度窗口。数据库当时共有 336 个历史窗口，
  其中 6 completed、6 split、324 pending，正式官方文档与案例四表仍为 0 行。
- 第一段正式实现确定为 1—3 个真实案例的纵向切片：线索/官方扫描 → 官方候选文档 →
  结构化案例 → 关键字段官方引文回验。该切片不降低 50 份官方候选、10 个正式案例和
  候选精确率不低于 90% 的生产上线闸门。

## 阶段边界

- 本次只定稿设计口径并同步已验收 Demo；正式采集器、数据库写入与生产前端尚未修改。
- 按需求梳理流程，设计文件提交后由用户复核书面口径；确认后编写实施计划并以 TDD
  开发第一条真实案例链路。

## M6 媒体线索公开边界修订

- 用户确认：在官方文件缺失时，可以公开展示有明确来源的媒体报道，但必须标注
  “媒体报道／未核实”。该信息用于综合研究，不代表网站对报道内容作出事实或监管认定。
- 采用统一工作区、双数据层方案。官方四表继续承载正式案例；原始 Tavily 候选继续私有；
  另建经过自动门槛清洗的公开媒体投影，避免把全部搜索噪声或内部审计字段暴露给前端。
- 公开媒体门槛要求匹配上市公司、同一语境出现衍生品与已发生风险、具有明确媒体/日期/
  HTTPS 来源，并排除假设性提示、论坛、股吧、匿名自媒体和无发布主体页面。
- 前端新增证据级别筛选，正式案例与媒体线索分别统计和导出。媒体升级为正式案例后只保留
  正式主行，媒体转为补充来源，避免重复计数。
- 本次只修订书面规格；Demo、数据库、采集器与生产前端等待用户复核规格后再进入 TDD
  实施计划。

## M6 媒体未核实公开层实施与验收

- 已部署 `risk_media_reports`、`risk_media_report_sources` 及配套 RLS、权限、索引和清理/
  验证 SQL。公开表只暴露安全投影；原始 Tavily 候选和回填窗口继续保持私有。
- 确定性发布门槛已实现：公司匹配、日期、HTTPS、媒体白名单、局部衍生品词与已发生风险词
  缺一不可；官方域名、论坛/股吧/匿名来源、假设性风险和无法识别发布者均拒绝。该路径不
  抓取全文、不调用 MiniMax。
- 真实私有候选共 2 条：江特电机 2025-12-28 新浪财经报道通过，写入 1 条公开媒体报告和
  1 条来源；另一条因缺少公司代码/名称被拒绝。相同发布器连续写入两次后公开计数保持 1/1，
  幂等成立；私有候选原有复核状态未被覆盖。
- 匿名 REST 实测：公开报告与来源均返回 200，各 1 行；`risk_media_leads` 与
  `risk_media_backfill_windows` 均返回 401。Security Advisor 为 0 告警；Performance
  Advisor 没有缺失外键索引，只提示新空表索引尚未使用。
- 两条媒体工作流已在增量/历史写库后自动执行发布器；push 只跑测试，不调用 Tavily。
  本地风险 Demo 已加入官方/媒体证据筛选、分别计数、媒体来源详情与来源字段 CSV 导出；
  生产 `web/` 未修改，正式导航继续受原有质量闸门约束。
- 数据库迁移名称：`m6b_public_media_projection`、`m6b_public_media_official_case_index`。
  发布前远端回退基线：`749797e9b43d1415efa22c10be1b48f33c778faa`。
- 发布前完整回归：159 项 Python 测试通过；6 组离线 Node 前端测试通过；真实公开 API
  启动测试通过并读取 8,237 个事件、18,495 条结构化公告；19 个发布文件的密钥值扫描
  无命中。生产 `web/` 不在本次提交范围。

## 下一步

- 独立推进 1—3 个官方案例纵向切片，优先从已知媒体事件反查交易所、证监会/派出机构或
  法定披露原文，并补齐 SZSE、CSRC、CNINFO 来源适配器。
- 每个正式案例的公司、日期、风险类型、金额、监管动作与结论字段必须逐项绑定可定位官方
  引文；未被官方原文覆盖的字段保持“未披露”。

## 公告事件额度来源回退修复

- 用户抽查发现鹏辉能源 2026 事件显示额度和期限“未披露”。数据库核验确认 4 月原计划和
  7 月调额公告均已正确提取；旧 `build_events.py` 选择 7 月 24 日无金额股东会决议作为
  `quota_source_ann_id`，是事件聚合层丢失，不是模型抽取失败。
- 首轮审计发现 44 个同类事件（其中 39 个为 2026 年），成员公告已有 154 条额度行但事件
  快照为空。修复后进一步用“已回验金额是否仍被隐藏”审计，发现 16 个事件被非空额度行中
  的 null 金额遮蔽；据此把来源条件收紧为至少一条金额非空且 `amount_verified=true`。
- 事件阶段继续取最高审批层级；额度、期限等展示字段回退到同事件最近的可验证额度公告。
  额度快照新增保留 `page` 与 `quote_verified`，全事件没有可验证金额时仍保留最高审批公告
  的纯文本额度行供复核。
- 两次 TDD 红绿循环新增 3 项回归测试。最终生产重建得到 8,237 个事件、18,495 个成员，
  孤儿成员 0、相关公告未挂靠 0、可恢复额度仍为空 0、已回验金额仍被隐藏 0；未调用 LLM。
- 鹏辉能源 2026 事件保持“股东大会通过”，额度来源变为 2026-07-09 调额公告
  `1225414749`，恢复 8 条商品/外汇/综合额度及有效期限。
- 完整回归：162 项 Python 测试、6 组离线 Node 测试和真实 API 启动测试全部通过；真实
  启动读到 8,237 个事件和 18,495 条结构化公告。
