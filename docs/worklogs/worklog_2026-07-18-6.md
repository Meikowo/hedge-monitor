# Worklog 2026-07-18（Build Events 修复验收）

## 执行结果

用户在 PR #11 合并后手动运行 `Build Events`，workflow 成功。

Supabase 只读复核：

- hedge_events: `543`
- event_members: `1067`
- 多公告事件: `350`
- 孤儿挂靠: `0`
- 套保相关抽取未挂靠事件: `0`
- announcements: `extracted=1067`、`pending=2378`、`failed=85`、`irrelevant=1`、`skipped=1`

## 结论

事件主键修复有效，事件派生层已完整恢复，可以继续清理抽取积压。

## 下一步

运行 `Extract Batch (LLM)`：`limit=300`、日期留空、`retry_failed=false`。每次只运行一批并等待完成；pending 清零后再处理 failed。
