# Worklog 2026-07-28

## 本阶段目标

完成 30 份 2025FY POC 的分层人工验收，固化边界规则，并在不覆盖人工金标准的前提下
启动 100–200 家优先池扩量。

## 人工验收结论

1. 辉隆股份：人工确认未应用套期会计；报告未披露原因。
2. 生益科技：`处置损益 -9,950,713.48 元` 是独立的已实现/处置损益，不是综合损益。
3. 航民股份：黄金租赁保证金属于租赁/融资担保，排除；黄金/白银 T+D 投资收益纳入
   衍生品实际损益，但套保目的保持未明确。
4. 中稀有色：虽审议 2025 年期货套保计划，但未找到实际开展证据；普通交易性金融资产
   收益及衍生金融工具公允价值变动均不作为套保实际损益。
5. 博彦科技：报告期不存在衍生品投资；通用套期会计政策是政策披露，不代表实际使用。

## 数据库处理

- 写入前完整备份五份报告的 `periodic_derivatives`、`periodic_metric_items` 和
  `periodic_hedge_accounting_items`。
- 以单一事务完成更新；五份 `review_status` 均改为 `accepted`。
- 指标总数由 198 校正为 197：删除航民黄金租赁保证金、删除中稀非套保指标、增加航民
  T+D 投资收益，并调整生益处置损益类型。
- 复核结果：30 份报告、197 条指标、未通过数字或引文回验 0、披露状态需复核 0。

## 规则与测试

- 提示词升级为 `periodic-v2.4-reviewed-boundaries`。
- 新增黄金租赁保证金排除、T+D 收益保留并归入商品范围、生益处置损益重分类、
  仅有衍生工具附注时进入复核、未明确套期会计不得保留应用类型等规则。
- `review_status=accepted` 默认阻止模型覆盖；显式 `--force-reviewed` 才允许重跑。
- `tests.test_periodic` 共 89 项全部通过。

## 120 家扩量

- 建立 `config/annual_priority_2025.csv`：商品、外汇、商品+外汇各 40 家，包含原 30 家。
- 选择器新增公司主表外键检查；排除 64 个不满足主表外键的事件视图候选。
- 元数据标题兼容“2025年年度报告”和“2025年度报告”；同公司同期只保留修订优先、
  日期最新的规范报告。
- 120/120 家取得 2025FY 年报元数据；得邦照明旧版标记为 skipped。
- 首批 10 份候选页定位成功，无 OCR 或空候选页；新增样本已有 7 份完成模型抽取。
- 首次抽取在爱柯迪遇到仅含 `<think>`、清洗后无 JSON 正文的响应，未写入数据库。
  新增异常分类：无 JSON/仅思考响应允许退避重试；已经开始但括号未闭合的 JSON 仍
  立即停止，防止重复产生长输出费用。定期报告调用现显式关闭 thinking，不依赖本地
  环境变量是否存在。
- 新增 7 份为中国船舶、宝钢股份、爱柯迪、德创环保、天奈科技、东方盛虹和分众传媒。
  共新增 53 条数值事实；数字与引文未回验 0、语义重复 0、套期会计明细待复核 0。
- 首批探针暴露并修复三个可泛化问题：
  1. `√适用 □不适用` 曾被宽松正则反向识别；现仅在“不适用”明确被勾选或明文写为
     不适用时判定未应用。中国船舶据 P214–215 校正为已应用，外汇为公允价值套期、
     商品为现金流量套期。
  2. 标准表单位新增“百万元”；宝钢 P28 的商品/货币衍生品数值已按百万元保存，
     净资产比例仍保持 `%`。
  3. 科目名前的“（九）”等编号在语义去重键中忽略；宝钢重复的衍生金融负债事实已删除。
- 东方盛虹标准衍生品表新增“投资收益”列映射，补入商品 `16,587.42 万元` 和外汇
  `614.24 万元` 两条报告原文事实。

## 下一步

首批 7 份自动质量审计通过，但人工验收状态仍保持 pending。下一批继续处理已定位的
珠海港、广宇集团和 *ST 生物；随后按 5–10 份定位、2–6 份抽取的小批次推进。只有出现
业务边界、复选框冲突、单位异常或勾稽不成立时才提交人工问题，不要求逐份通读。
