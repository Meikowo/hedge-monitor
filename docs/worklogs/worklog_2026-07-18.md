# worklog_2026-07-18 - M3 前端真实数据预览

## 1. 本次阶段

在 R1 历史抽取仍进行中的同时，启动 M3 前端预览，避免必须等待全部公告处理完才看到产品形态。

## 2. 已完成

- 新增 `web/` 静态前端，采用浅白纸面、墨色正文、陶土色强调和衬线标题，遵循 PRD 7.6 的“研报纸感的数据终端”。
- 接入 Supabase 公开只读视图 `v_ann_flow` 与 `v_events`，使用 publishable/anon key；service role key 未进入前端。
- 完成事件面板、公告原流、公司/代码搜索、类别与公告角色筛选、事件详情抽屉、额度口径表、证据引文和 PDF 跳转。
- 完成移动端堆叠布局，保证高信息密度下的阅读留白。
- 新增 `.github/workflows/pages.yml`，合并至 `main` 后自动发布 GitHub Pages。
- 已用公开只读请求验证两个 Supabase 视图均返回 HTTP 200，JavaScript 语法检查通过。

## 3. 当前部署状态

- 代码已合并到 GitHub `main`。
- GitHub Pages API 当前返回 404，说明仓库 Pages 尚未启用；需要在仓库 Settings → Pages 将 Source 设置为 `GitHub Actions`。
- 启用后重新运行 `Deploy Hedge Monitor Preview`，预计地址为 `https://meikowo.github.io/hedge-monitor/`。

## 4. 下一步

1. 用户启用 GitHub Pages 的 GitHub Actions 来源。
2. 验证页面真实加载 708 条已抽取数据和 362 个事件。
3. R1 抽取继续后台推进；前端无需因数据增长重新设计。
