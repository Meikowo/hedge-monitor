# Worklog — 2026-07-21

## 本次目标

- 在套保事件前端完整补出公司所在省份。
- 为数据看板增加省份统计图。
- 让年度趋势之外的看板图表可以统一按年份筛选。
- 将本地尚未同步的 M4a POC 与本次 M3 改动合并为一次可审计提交。

## 已完成

1. 核对 Supabase 只读视图：`v_events` 与 `v_ann_flow` 已包含 `province`，无需数据库迁移。
2. 事件表与公告表新增独立“省份”列，详情抽屉新增省份字段，全文搜索可匹配省份。
3. 事件与公告 CSV 导出新增“省份”列，继续采用 UTF-8 BOM，保留表格软件直接打开的中文兼容性。
4. 数据看板新增“省份覆盖 Top 16”，按股票代码去重统计公司，并同时显示事件数。
5. 新增看板统一年份选择器：企业性质、类别、行业、省份、审批层级和字段质量随年份切换；年度趋势不受筛选影响。
6. 真实数据核验：
   - 2025：1,464 个事件、1,321 家公司、1,443 个事件具备省份字段；
   - 2026：1,812 个事件、1,635 家公司、1,808 个事件具备省份字段。
7. 增加 `tests/test_web_structure.mjs`，校验前端脚本引用的 DOM 节点、两张表的省份列及看板省份/年份控件。

## 数据与口径说明

- 省份来自公司主数据，而不是 LLM 抽取，不增加模型费用和运行时间。
- 看板年份筛选只改变客户端统计口径，不写库、不改变公告和事件数据。
- 少数省份为空时显示“未录入”；不会将缺失值伪装成具体地区。

## 下一步

- 用户人工核查首批年报 POC 后，再决定 M4a 是否从 2 份扩至 30 份。
- 随历史年份继续补齐，可复核各年份省份缺失率，并视数据量决定是否加入地图或省份下钻。
