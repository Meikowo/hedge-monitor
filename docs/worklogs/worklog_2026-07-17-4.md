# worklog_2026-07-17-4 - LLM API 密钥安全切换

## 1. 本次阶段

处理 MiniMax API 密钥误用按量计费密钥的问题，确保后续本地运行与 GitHub Actions 使用 token plan 密钥。

## 2. 已完成

- 本地 `.env` 中 `LLM_API_KEY` 已存在新值；仅检查变量存在和长度，没有读取或输出密钥内容。
- 本地 `.gitignore` 已加强：忽略 `.env` 与 `.env.*`，保留 `.env.example`。
- GitHub 仓库原本已忽略 `.env`，未发现需要删除的已提交密钥文件。

## 3. 必须由用户完成

- 在 GitHub 仓库 Settings → Secrets and variables → Actions 中，将 `LLM_API_KEY` 更新为 token plan 密钥。
- 在 MiniMax 控制台撤销旧的按量计费密钥，避免误用或泄露风险。

## 4. 结论

在 GitHub Secret 更新并撤销旧 key 前，不再运行新的抽取批次。更新后继续使用 `limit=300`、日期留空、`retry_failed=false`。
