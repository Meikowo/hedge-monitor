# worklog_2026-07-13 —— R0：从头重建项目底座

## 1. 事件与背景

用户决定新项目从头开始，不再增量修补旧 demo。本次会话输入：PRD v1.2、旧
PROJECT.md/COLLAB_SOP、旧代码包（hedge-monitor.zip）、iFind 全部A股公司表
（20260711 导出，5524 家有效）。对三个既定问题（查全率 / 事件去重 / 额度口径）
用户已给出方向性裁决：全自动解决、数据分层、结构化口径字段。

## 2. 决策（已同步 PROJECT.md §4 ADR）

1. **从头重建，旧数据不迁移。** 新词表要求重新召回、新模型要求重新抽取，
   迁移旧 124 条抽取无价值。旧代码存档 legacy-demo 分支。
2. **三层数据模型定稿**：announcements（公告层）→ extractions + quota_items
   （抽取层）→ hedge_events + event_members（事件层，派生全量重建）。
3. **查全率三层召回**：L1 标题 13 词逐词查+去重；L2 月度全文审计自动补捞入库
   （audit workflow）；L3 LLM is_hedge_related 兜底。零人工。
4. **抽取角色化**：is_hedge_related + ann_role 八分类替代旧单布尔，制度/可行性/
   进展全收但不混入计划统计；仅计划类公告贡献事件额度。
5. **口径落 quota_items**：basis 六值闭集 CHECK；每条额度带原文+页码+双程序回验
   （amount_verified / quote_verified）——PRD 5.7 第二层防线提前到公告管线。
6. **MiniMax 定稿并复核**：api.minimaxi.com/v1，MiniMax-M3，thinking adaptive
   （与官方 2026-06 文档一致）；Actions 可达性待 probe 实测。
7. **companies 定源 iFind**，季度手动导出 + workflow 导入；企业性质映射
   央企/地方国企/民企/外资/集体/其他，原值保留 ent_nature_raw。
8. **巨潮任务串行化**：daily/backfill/audit 共用 concurrency group=cninfo。

## 3. 交付（本 zip 全部内容 = 新 main 全量）

代码：scripts/{common,cninfo,fetch_announcements,audit_recall,prompt_extract,
extract_announcements,build_events,import_companies,probe_minimax}.py；
配置：config/keywords.yml、.env.example、requirements.txt、.gitignore；
数据库：db/{000_reset,001_init,verify}.sql；
调度：.github/workflows/ 六个（daily/backfill/extract/audit/import-companies/probe）；
文档：README + docs/{PRD,PROJECT,COLLAB_SOP,schema_snapshot} + 本 worklog；
数据：data/companies_ifind_20260711.xlsx（入仓，季度替换）。

本地已验证：全部 py 编译通过；import_companies 对真实 xlsx dry-run 通过
（5524 行，性质/市场分布与源数据一致）；JSON 容错解析与金额回验单测通过；
全部 YAML 语法通过。巨潮与 MiniMax 网络调用无法在交付环境实测，
依赖旧包已验证参数 + probe workflow 线上实测。

## 4. 验收（用户执行，README「首次部署」8 步）

关键指标：V1/V2 六表 RLS+策略齐；V3 companies=5524；V4 回填 2026 后数千条且
pending 随 extract 轮次趋零；V7 amount_verified=false 占比 <10%；
V8 事件数 < 相关公告数且多公告事件占比可观；V9 两项孤儿=0。

## 5. 回滚说明

000_reset 为销毁性操作，但全部数据可由管线重建（companies←xlsx、公告←backfill、
抽取←extract 重跑，重抽消耗 MiniMax token 属预期成本）。旧库若想保险，可先在
Supabase 后台做一次项目备份/导出再执行 reset。

## 6. 教训与备忘

- 逐源实测原则继续有效：MiniMax@Actions 未测前，daily 的抽取步骤设为
  continue-on-error，保证采集与事件层不受抽取可达性影响。
- 事件分组 v1 是启发式（同年同类并事件、450 天进展窗），派生表设计让规则演进
  零成本；R1 出真数据后应抽 20 个多公告事件人工核对分组正确性。
- 下一步顺序：部署 8 步 → probe 结论回贴 → 回填 2026 + 清积压 → verify 全量
  回贴收口 R1 → 逐年 2025→2021（R2）→ 金标准评测（R3）→ 前端（M3）。
