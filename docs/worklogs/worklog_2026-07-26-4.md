# 工作日志 2026-07-26（第四阶段）

## 本阶段主题

根据中集集团与株冶集团年报截图完成 M4a 证据规则复核，并由程序自行下载、渲染原始 PDF
页面验证页码与表格内容。

## 核验结论

- 程序从巨潮下载并渲染了株冶 P30、中集 P48 和 P197；人工截图不是常规依赖。
- 中集 P197 的“人民币千元”无误，数值验证已通过；引文失败来自 PDF 表格扁平化后
  行列关系丢失，模型重构摘录无法逐字匹配。
- 中集 P48 的货币互换行只有净资产占比 `0.00%` 是真实零；买入额、售出额和期末余额
  均为横线。数据库中 3 条错误金额零已删除。
- 株冶 P30 在定位器候选集内，但 LLM 漏抽了明确披露的报告期损益；并非定位漏页。
- 株冶 P31 旧结果同时存在单位换算和指标错分：最高保证金被标成最高名义本金，
  `亿元/亿美元` 被换算为基础单位，其中 `5.45亿元` 还被错误放大 10 倍。

## 规则与数据修复

- 提示词升级为 `periodic-v2.2-evidence`，覆盖“报告期实际损益情况”，并强制保留
  千元、万元、亿元、万美元、亿美元等原文单位。
- 增加原文缩放单位恢复、最高保证金纠错、明确平仓与持仓损益句式兜底。
- 增加零值单位约束：百分比零不能验证货币金额零。
- 选择性短批次空返回默认保留旧事实，防止偶发空响应删除已有数据。
- 株冶数据库现有 10 条数值事实，其中 P30/P31 四条双回验：
  - 商品衍生品报告期损益 `-366,065,852.86 元`；
  - 商品最高保证金 `5.45亿元`；
  - 商品最高合约价值 `32.70亿元`；
  - 外汇最高合约价值 `1.7亿美元`。
- 株冶 P29 买入、售出、期末账面价值和比例，以及 P148/P189 资产负债数值验证通过，
  但复杂表格引文仍需后续单元格坐标方案。

## 验证

- `tests.test_periodic`：27 项全部通过。
- Token Plan 仅重跑株冶 `pnl` 与 `operations` 短批次；损益兜底成功写入。
- 数据库回查确认株冶共 10 条事实，修正后的 P30/P31 四条均
  `value_verified=true` 且 `quote_verified=true`。
- 中集错误零值删除后为 19 条事实、8 条双回验；其余未双回验事实保持待复核，
  未人为放宽引文校验。

## 下一步

继续 6 份人工验证：优先修复复杂表格单元格坐标与行列证据，再核验大北农和博彦科技
的“无数值”判定。规则稳定后才扩展到 30 份 POC。

